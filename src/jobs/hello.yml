description: >
  Sample description
# What will this job do?

docker:
  - image: cimg/base:current

steps:
  - checkout
  - setup-dependencies
  - cli-instrument
  - artifacts:
      paths:
        - my_var.txt
      when: always
      post:
        - echo ${WORKFLOW_ID} > my_var.txt
  - setup_remote_docker:
      version: 20.10.14
  - run-image
  - cleanup
