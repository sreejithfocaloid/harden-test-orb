description: >
  Sample description
# What will this job do?

docker:
  - image: cimg/base:current

steps:
  - checkout
  - setup-dependencies
  - cli-instrument
  - save_cache:
      key: my-cache
      paths:
        - my_var.txt
  - setup_remote_docker:
      version: 20.10.14
  - run-image
  - cleanup
