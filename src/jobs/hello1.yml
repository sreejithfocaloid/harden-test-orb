description: >
  Sample description
# What will this job do?

docker:
  - image: cimg/base:current

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - checkout
  - setup-dependencies
  - restore_cache:
      keys:
        - my-cache
  - cli-harden
